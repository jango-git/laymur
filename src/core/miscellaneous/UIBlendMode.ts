/** Color blending modes */
export enum UIBlendMode {
  /** Standard alpha-over compositing. */
  NORMAL = 0,
  /** Additive blending (light accumulation). Only affects BLEND transparency. */
  ADDITIVE = 1,
}
