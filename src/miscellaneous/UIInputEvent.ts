/**
 * Events that can be emitted by UI elements.
 */
export enum UIInputEvent {
  DOWN = "pointerdown",
  MOVE = "pointermove",
  UP = "pointerup",
  ENTER = "pointerenter",
  LEAVE = "pointerleave",
}
