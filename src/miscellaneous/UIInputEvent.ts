/**
 * Events that can be emitted by UI elements.
 */
export enum UIInputEvent {
  PRESSED = "pointerdown",
  MOVED = "pointermove",
  RELEASED = "pointerup",
  ENTERED = "pointerenter",
  LEFT = "pointerleave",
}
