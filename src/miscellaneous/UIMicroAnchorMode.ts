/**
 * Anchor mode for UIMicro transformations.
 *
 * @public
 */
export enum UIMicroAnchorMode {
  /** Rotation and scaling applied around the anchor point */
  ROTATION_SCALE = 0,
  /** Position, rotation, and scaling applied around the anchor point */
  POSITION_ROTATION_SCALE = 1,
}
